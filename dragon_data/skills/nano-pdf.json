{
  "meta": {
    "name": "nano-pdf",
    "description": "Edit and create PDFs using natural-language instructions via nano-pdf CLI. Supports nano-pdf create (from markdown) and nano-pdf edit (modify existing). Hermes-aligned.",
    "version": "1.0.0",
    "tags": ["PDF", "Documents", "Editing", "Create", "NLP", "Productivity"],
    "execution_mode": "sequential",
    "source": "hermes"
  },
  "content": "# nano-pdf\n\nEdit and create PDFs using natural-language instructions. Point it at a page and describe what to change — or create a new PDF from markdown.\n\n## Prerequisites\n\n```bash\n# Install with uv (recommended — already available in Hermes)\nuv pip install nano-pdf\n\n# Or with pip\npip install nano-pdf\n```\n\n## Usage\n\n### Edit an existing PDF\n\n```bash\nnano-pdf edit <file.pdf> <page_number> \"<instruction>\"\n```\n\n### Create a new PDF from markdown\n\n```bash\nnano-pdf create <input.md> <output.pdf>\n```\n\nWrite the markdown file first with `write_file`, then call `nano-pdf create` via `terminal` to generate the PDF. The markdown supports headings, lists, code blocks, bold, italic — standard formatting. CJK (Chinese) text is handled natively.\n\n## Examples\n\n```bash\n# Create a PDF from markdown\nnano-pdf create report.md report.pdf\n\n# Change a title on page 1\nnano-pdf edit deck.pdf 1 \"Change the title to 'Q3 Results' and fix the typo in the subtitle\"\n\n# Update a date on a specific page\nnano-pdf edit report.pdf 3 \"Update the date from January to February 2026\"\n\n# Fix content\nnano-pdf edit contract.pdf 2 \"Change the client name from 'Acme Corp' to 'Acme Industries'\"\n```\n\n## Notes\n\n- Page numbers may be 0-based or 1-based depending on version — if the edit hits the wrong page, retry with ±1\n- Always verify the output PDF after editing (use `read_file` to check file size, or open it)\n- The tool uses an LLM under the hood — requires an API key (check `nano-pdf --help` for config)\n- Works well for text changes; complex layout modifications may need a different approach\n"
}
