[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dragon-agent"
version = "1.2.0"
description = "🐉 Dragon Agent — Self-Evolving AI Agent Framework"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.11"
authors = [
    {name = "jialine"}
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "click>=8.0",
    "rich>=13.0",
    "httpx>=0.27",
    "openai>=1.50",
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "python-dotenv>=1.0",
    "tenacity>=8.0",
    "networkx>=3.0",
]

[project.optional-dependencies]
compressor = [
    "tiktoken>=0.5",
]
feishu = [
    "lark-oapi>=1.5.3",
    "qrcode>=7.0",
]
telegram = [
    "python-telegram-bot>=20.0",
]
discord = [
    "discord.py>=2.0",
]
wechat = [
    "wechatpy>=1.8",
]
tui = [
    # TUI frontend is Node.js; no Python deps needed
]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "pytest-cov>=4.0",
]

[project.scripts]
dragon = "dragon.cli:main"

[project.urls]
Homepage = "https://gitee.com/jialine/dragon-agent"
Repository = "https://gitee.com/jialine/dragon-agent"

[tool.setuptools]
packages = [
    "dragon",
    "dragon.backup",
    "dragon.compressor",
    "dragon.debate",
    "dragon.dispatch",
    "dragon.explorer",
    "dragon.gateway",
    "dragon.guard",
    "dragon.jury",
    "dragon.mcp",
    "dragon.memory",
    "dragon.plugin",
    "dragon.provider",
    "dragon.router",
    "dragon.skill",
    "dragon.tool",
    "dragon.tool.builtins",
    "dragon.tui",
    "dragon.utils",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
